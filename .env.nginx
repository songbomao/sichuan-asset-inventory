# 同域部署专用 env（npm run build --mode nginx）
# 显式将 VITE_API_BASE_URL 置空，配合 src/api/client.ts 中的回退逻辑，
# 使前端运行时自动使用 window.location.origin（当前域名），由 nginx 反代 /api，
# 从而彻底消除跨域，规避 WAF 对 CORS 预检 OPTIONS 的拦截（钉钉手机端 Network Error）。
# 注意：Vite 始终会加载基础 .env（其中含绝对 URL），因此必须在此显式覆盖为空。
VITE_API_BASE_URL=
